class Pipe {
    constructor() {
        // TODO: Definir parámetros de la tubería
    }

    update() {
        // TODO: Definir el método que se llamará para actualizar la posición del elemento tubería
    }


    show() {
        // TODO: Definir el método que se llamará para dibujar al elemento tubería
    }

    // Utility method
    updateAndShow() {
        this.update();
        this.show();
    }

    hits(bird) {
        // TODO: Método que define si una tubería ha colisionado con un elemento pájaro
        // Debe tener un elemento pájaro como argumento (bird) y devolver Bool
    }
}

